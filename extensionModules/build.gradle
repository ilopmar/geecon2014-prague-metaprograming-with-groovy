apply plugin: 'groovy'
version = "1.0"
jar.archiveName = "string-extensions-${version}.jar"

repositories.mavenCentral()

dependencies {
    compile 'org.codehaus.groovy:groovy:2.3.7'
    testCompile 'org.spockframework:spock-core:0.7-groovy-2.0'
}